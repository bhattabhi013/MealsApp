import 'package:flutter/material.dart';

class FavouritesSceen extends StatelessWidget {
  const FavouritesSceen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("hy"),
    );
  }
}
